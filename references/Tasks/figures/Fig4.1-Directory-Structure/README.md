# Fig 4.1 — Project directory structure
Open Abserny-Mobile in VS Code. Screenshot the Explorer sidebar
showing the full folder tree with hooks/, assets/, App.js, etc.
Or run from the project root:
  find . -not -path '*/node_modules/*' -not -path '*/.git/*' | head -50
Filename to use: fig_4_1_directory_structure.png
